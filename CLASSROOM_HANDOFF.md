# 下一节课从这里继续｜Adventure World 彩虹探险乐园

更新：2026-09-17（Australia/Sydney）。本文件记录独立终版复核与课堂接续，不替代SPEC中的设计来源。

## 1. 当前结论和固定版本
**PASS_CORE / ACCEPT_WITH_FOLLOWUPS：核心完整游玩流程通过，可作为下一节课的试玩和迭代基线；中文覆盖和实体iPad验收尚未收尾，不等于SPEC所有条款无条件通过。**
受测代码提交：`392ed683daeb5e15ab89194c4ad6d491ad526589`，包含`aac8dc3`的点击移动/离线bundle改动。本轮不改游戏逻辑、不启动开发Worker，只补文档和证据。
这是Joey、Mia、Chloe共创的单机乐园探索游戏，不是Martin的抓宠战斗，也不是多人联机项目。保持免费游园、飞行、隐形、手电和小狗寻线索的原方向。

## 2. 本次独立验收结果
在独立Chromium上下文通过HTTP启动。完整路线只使用真实键盘、指针和UI按钮；没有传送角色、注入任务/胜利状态、修改数值或加速计时。
| 检查 | 结果与证据 |
|---|---|
| 标题输入冻结、选择外观、开始 | 通过；标题按方向不移动，选择avatar-2后进入PLAY。 |
| 彩虹滑梯 | 通过；实际乘坐到出口(540,440)，获得waterpark印章。 |
| 动物园 | 通过；在公共步道依次观察lion、crocodile、giraffe，获得zoo印章。 |
| 狗零食、抚摸、喂食 | 通过；完成互动后白天印章3/3。 |
| 主动夜游与小狗嗅探 | 通过；进入NIGHT，等待实际脚印路线完成，亲自走到终点照射并取得trail。 |
| 飞行与手电 | 通过；越过低障碍，照射观察点后取得sky。 |
| 隐形与手电 | 通过；在猫头鹰观察点隐形、持续照射后取得quiet。 |
| 最终控制台与胜利 | 通过；收集3条线索后返回控制台互动，出现VICTORY，印章3/3、线索3/3。 |
| 胜利后继续游园 | 通过；进入COMPLETED且界面回到PLAY。 |
| 暂停 | 通过；暂停中持续按方向，坐标不变；可恢复。 |
| 存档与刷新 | 通过；COMPLETED、3印章、3线索、外观与中文设置在刷新后保留。 |
| 确认后重新开始 | 通过；只清本游戏进度，回到DAY、0印章、0线索。测试使用独立浏览器上下文。 |
| 点击地面移动 | 通过；从(650,950)点击目标后自然移动至约(726,950)。 |
| 中文切换 | 部分通过；locale切到zh并持久保存，但胜利计数仍为“Stamps: 3/3 / Clues: 3/3”，见AW-01。 |
| WebKit触屏/离线入口 | 基础冒烟通过；768×1024触屏配置从file://启动、进入PLAY并发动飞行；另保存1024×768截图。不是实体iPad性能认证。 |
| 现有单元测试 | 重新执行`node tests/unit-tests.js`，10/10通过。 |
| bundle与源码一致性 | 在隔离目录复制src/data/scripts并重新构建，生成bundle.js与已提交文件逐字节相同。 |
本次主线Chromium和基础WebKit触屏测试未捕获pageerror。浏览器版本：Chromium 145.0.7632.6、WebKit 26.0。完整证据见[docs/qa/2026-09-17](docs/qa/2026-09-17/)。
测试脚本最初在长颈鹿交互范围外停下，调整实际走位后成功；夜游按钮持续动画使自动等待稳定超时，改为在真实按钮屏幕中心点击后成功。两项属于测试操作修正，不冒充产品故障，也未绕过游戏规则。

## 3. AW-01｜P2：补齐真正的中英双语
实际切换到zh后，胜利汇总DOM仍显示英文Stamps/Clues。`src/quests.js`中的若干印章/线索提示、夜间能力提示也直接拼英文；不是只翻译首页就完成了SPEC第8节。
定位：`src/main.js:updateUIOverlays`、`src/quests.js:unlockStamp/unlockClue/getInteractionPrompt`及`data/locale.en.json`、`data/locale.zh.json`。修改后必须重建bundle。
验收标准：使用中文从新建走完白天、夜游和结局；任务、能力、提示、夜游门槛和汇总文字均走词条；刷新后语言与任务进度保留。

## 4. AW-02｜P2：课堂触屏与显示收尾
WebKit模拟的横竖屏可启动、点按钮和发动飞行，但并未验证真实iPad硬件、Safari工具栏、双指摇杆加技能、长时间性能。
竖屏截图中底部操作说明条紧贴并部分叠到技能按钮下方标签；应为说明条与按钮分别预留安全区。不能把“保存了截图”等同于所有触控区域都验收通过。
开课前实际设备检查：横竖屏、暂停/返回、持续移动加飞行或隐形、切后台恢复、按钮不被工具栏遮挡。只修适配，不重做世界或改为3D。

## 5. AW-03｜测试与构建批注
旧`tests/browser-e2e.js`多次直接修改玩家坐标，并使用`--disable-web-security`；它可用于组件场景验证，但不能单独证明真实路径连通或正常浏览器安全策略下的完整游玩。
本次另行走了无传送路线，不能据此把旧测试升级为全覆盖认证。现有unit suite的10/10也不等于A01–A16所有浏览器验收全部通过。
后续保留一条不改position/stamps/clues/win、不关闭浏览器安全策略的主线测试；强制夹具测试须单独标记，失败时脚本返回非零状态。
`index.html`使用`bundle.js`。src/data是修改入口，不直接手工改bundle。改变任一src或data文件后运行`node scripts/build-bundle.js`，将源码和生成文件一起提交，避免在线/离线行为分叉。

## 6. 可复现的普通游玩路线
新建→滑梯入口附近(285,210)互动→出口(540,440)→公共步道分别靠近lion(980,290)、crocodile(1200,270)、giraffe(1360,290)互动→绕过小狗围栏到零食摊(160,800)→靠近小狗抚摸/喂食。
三枚印章后主动夜游→嗅探起点(320,840)互动→等待路线揭示→终点(140,640)照射至少1秒后互动→低障碍前起飞至(180,180)，照射后互动→(920,480)隐形并照射后互动→返回(650,840)控制台→胜利。
坐标只是复现导航，不是传送指令。沿合法地面绕开高围栏；交互需进入48单位范围。夜间线索顺序本次为trail→sky→quiet，其他顺序没有在本轮逐一实走。

## 7. 开课前接续步骤
1. 读本文件→README→SPEC；核对分支、HEAD和未提交改动。SPEC顶部NOT_STARTED是编制时的历史状态，不是今日实现状态。
2. 从仓库根目录启动；若已有8890服务，确认它服务的是本仓库，不额外重启或按端口盲目结束别人的进程。
3. 先修AW-01与AW-02，再把真实主线补进可重复测试。保持现有单人2.5D、免费游园和温和夜游，不添加登录、联机或付费依赖。
4. 开课时先让学生试玩当前基线，再记录本节课确认的一项改动；不要因老师打开仓库就自动扩大开发任务。
5. 开发完成交回新commit、改动清单、原有主线回归、横竖屏图和未测边界；长任务一次领取、完成回收，不高频轮询。

## 8. 启动和生成
```sh
# 在本仓库根目录；修改src或data后重建
node scripts/build-bundle.js
node tests/unit-tests.js
python3 -m http.server 8890
```
HTTP入口为`http://127.0.0.1:8890/`；同Wi-Fi iPad使用本次开课时查到的Mac局域网IP。不要把README中的历史IP当作永久地址。
`index.html`直接file://启动也已完成WebKit基础验证。游戏运行不需要node_modules；Node用于构建/测试。浏览器测试依赖按package-lock安装，不提交node_modules。
源码地图：main.js负责循环/UI/点击移动；state.js负责阶段与能力；world.js负责碰撞；quests.js负责印章/线索；puppy.js负责小狗；save.js负责本地存档；render.js负责画面；input.js负责键盘/摇杆。

**下节课接续口令：读取本仓库CLASSROOM_HANDOFF.md，保持已通过主线不回退，优先补齐AW-01中文和AW-02触屏安全区；改src/data后重建bundle，交回实际游玩与存档/重开回归结果。**
本次没有认证所有线索排列、后台恢复、存储被禁用时的完整流程或真实iPad性能；后续测试需继续补齐这些边界。
